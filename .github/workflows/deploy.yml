name: Scheduled Render Deploy

on:
  schedule:
    - cron: '*/10 * * * *'  # UTCで10分おき（JSTなら+9時間）

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render Deploy Hook
        run: curl -X POST https://api.render.com/deploy/srv-d2o4t3mr433s73aul3o0?key=s3ZKvWbLPIE
